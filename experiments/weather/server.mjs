import {createServer} from 'node:http';import {readFile} from 'node:fs/promises';import {extname,join,normalize} from 'node:path';const root=process.cwd();createServer(async(req,res)=>{try{const p=normalize(join(root,new URL(req.url,'http://x').pathname));if(!p.startsWith(root))throw 0;const file=p.endsWith('/')?join(p,'index.html'):p;const data=await readFile(file);res.setHeader('content-type',extname(file)==='.js'?'text/javascript':extname(file)==='.html'?'text/html':'application/octet-stream');res.end(data)}catch{res.statusCode=404;res.end('not found')}}).listen(4173,'127.0.0.1',()=>console.log('ready'));
